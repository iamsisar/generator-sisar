language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sisar' ]; then cd .. && eval "mv $currentfolder generator-sisar" && cd generator-sisar; fi

