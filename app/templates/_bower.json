{
  "name": "<%= _.slugify(projectTitle) %>",
  "private": true,
  "dependencies": {
  	<% if (includeBootstrap) {  %>"bootstrap-sass-official": "~3.1.0",<% } %>
    <% if (includeModernizr) { %>"modernizr": "~2.6.2",<% } %>
    <% if (includeFontawesome) { %>"fontawesome": "~4.1.0",<% } %>
    "jquery": "<%= jqueryVersion %>"
  },
  "devDependencies": {}
}
